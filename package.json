{
  "name": "midiweb",
  "version": "1.0.0",
  "description": "웹 브라우저 기반 계이름 연주기",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
