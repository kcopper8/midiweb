# 자동 반주 기능 설계

## 개요

메트로놈과 연동되어 선택한 코드 진행을 자동으로 재생하는 기능입니다.

## 핵심 동작

- 메트로놈 시작 시 반주도 함께 시작 (반주 on 상태일 때)
- 메트로놈 정지 시 반주도 함께 정지
- 매 마디의 첫 박에 해당 코드의 구성음을 동시에 재생
- 4/4박자: 4박마다 코드 변경, 3/4박자: 3박마다 코드 변경

## 프리셋 (5종)

| ID | 이름 | 코드 진행 | 느낌 |
|----|------|-----------|------|
| pop | Pop | I-V-vi-IV | 밝고 대중적 |
| classic | Classic | I-IV-V-I | 전통적, 안정적 |
| sad | Sad | i-VI-III-VII | 어둡고 슬픈 |
| jazz | Jazz | ii-V-I-I | 세련된, 재즈 |
| fifties | 50s | I-vi-IV-V | 레트로, 올디스 |

## 기술 구현

### 코드 데이터 구조

```javascript
// 코드 타입 (루트 기준 반음 간격)
const chordTypes = {
  major: [0, 4, 7],      // 장3화음
  minor: [0, 3, 7],      // 단3화음
};

// 스케일 디그리 -> 반음 오프셋 + 코드 타입
const scaleDegrees = {
  'I':   { semitone: 0, type: 'major' },
  'ii':  { semitone: 2, type: 'minor' },
  'iii': { semitone: 4, type: 'minor' },
  'IV':  { semitone: 5, type: 'major' },
  'V':   { semitone: 7, type: 'major' },
  'vi':  { semitone: 9, type: 'minor' },
  'VII': { semitone: 11, type: 'major' },
  // 단조용
  'i':   { semitone: 0, type: 'minor' },
  'III': { semitone: 3, type: 'major' },
  'VI':  { semitone: 8, type: 'major' },
};

// 코드 진행 프리셋
const chordProgressions = {
  pop:     ['I', 'V', 'vi', 'IV'],
  classic: ['I', 'IV', 'V', 'I'],
  sad:     ['i', 'VI', 'III', 'VII'],
  jazz:    ['ii', 'V', 'I', 'I'],
  fifties: ['I', 'vi', 'IV', 'V'],
};
```

### 오디오 처리

`playChord(rootSemitone, chordType)` 함수:
- 코드 구성음 3개를 동시 재생
- 볼륨: 0.15 (메트로놈보다 낮게)
- 파형: sine (부드러운 소리)
- 음 길이: 마디 길이 (60/BPM * 박자수)

### 메트로놈 연동

- 박자 카운트 시 마디의 첫 박(beat === 0)에 코드 재생
- 코드 인덱스 순환 관리

## UI

### 레이아웃

메트로놈 행 우측에 추가:
- 반주 토글 버튼 (🎹 아이콘)
- 프리셋 드롭다운

### 상태

```javascript
accompanimentEnabled: false,
currentProgression: 'pop',
currentChordIndex: 0,
```

## 파일 변경

| 파일 | 변경 내용 |
|------|-----------|
| state.js | 코드 데이터, 반주 상태 추가 |
| audio.js | playChord() 함수 추가 |
| metronome.js | 마디 첫 박에 코드 재생 로직 |
| ui.js | 반주 컨트롤 렌더링 |
| main.js | 반주 이벤트 바인딩 |
| index.html | 반주 UI 마크업 |
| styles.css | 반주 UI 스타일 |

## 향후 확장

- 연주 따라가기 (사용자 연주에 맞춰 자동 화음)
- 프리셋 + 사용자 선택 방식
- 아르페지오 모드
